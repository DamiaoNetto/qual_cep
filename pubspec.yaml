name: qual_cep
description: "Consulta de CEP com Flutter Map"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  latlong2: ^0.9.1
  geocoding: ^3.0.0
  flutter_map: ^6.1.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true
  image_path: "assets/icon/QUAL_CEP.png"

flutter:
  uses-material-design: true
